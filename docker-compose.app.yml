services:
  # Frontend - Next.js
  nextjs:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: indiestack-app-nextjs
    restart: unless-stopped
    ports:
      - "3004:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://localhost:3005/api/v1
      - API_URL=http://go-api:3001
    depends_on:
      - go-api
    networks:
      - indiestack-network

  # Backend - Mock Go API
  go-api:
    build:
      context: ./backend
      dockerfile: Dockerfile.mock
    container_name: indiestack-app-api
    restart: unless-stopped
    ports:
      - "3005:3001"
    networks:
      - indiestack-network

networks:
  indiestack-network:
    external: true
